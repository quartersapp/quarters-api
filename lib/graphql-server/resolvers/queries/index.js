module.exports = {
  Query: {
    conversations: require('./conversations'),
    conversation: require('./conversation'),
    currentUser: require('./current-user'),
    showsForCity: require('./shows-for-city')
  }
}
